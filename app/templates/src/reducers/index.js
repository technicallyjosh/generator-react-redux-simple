import { combineReducers } from 'redux';
import { routerReducer as routing } from 'react-router-redux';

// here you would import reducers that you write and save in this directory

export default combineReducers({
    routing
});
